Read CLAUDE.md, the Phase 2 entries at the bottom of REVISION_LOG.md (from "PART 1: mathematical audit" down), and REVISION_LOG_THESIS.md before doing anything. Log everything to REVISION_LOG.md, appended, timestamped. Pre-register predictions before every run. No em-dashes anywhere.

Context you must hold. The SEDD linearization result (rho +0.184 pooled, +0.306 near, vs AR |rho| < 0.06) confirmed the training-objective diagnosis at the SIGNAL level. This phase adds the CAPABILITY level. The claim ceiling is fixed: a score-trained model natively performs the revision operation the AR gradient machinery failed at, the repaired direction signal works inside the thesis's own MH machinery, and trained noisy-state guidance steers where the AR constraint gradient did not. Best-of-N and SMC-style particle methods are EXCLUDED by decision: the thesis's introduction argues against sample-many-and-filter, so no experiment of that shape may appear under any name. Principled rate-matrix guidance (Nisonoff et al. 2024) is cited as future work, not attempted.

HARDWARE AND SHARDING (9 free A6000s; use them all). The last_token run took 153 minutes because one process ran all arms sequentially on one GPU. Do not repeat that. Every new experiment must accept --shard_idx and --num_shards over sequences. The manifest generator emits one job per experiment x model-scale x arm x shard, each shard with its own run_name and atomic JSON (the resume contract), plus a merge job per experiment that waits for its shard JSONs and assembles the final JSON and CSV with bootstrap CIs computed over the merged per-sequence rows. Use a fresh status dir. Classifier training (Part G) is one job on its own GPU. Target: the whole slate finishes in an afternoon of wall-clock.

PART 0: GATES AND UNIFICATION (before anything scales).

Variable-length gate. SEDD trains at fixed length 1024 with rotary positions; our sequences are 15 to 60 tokens. On 5 sequences, establish whether sedd-small accepts variable-length input directly. If it requires fixed length, pad, but verify the padding tail cannot condition the fill: clamp real positions and quarantine the tail so late-step MASK padding never sits as an off-distribution neighbour of the target. Document which regime holds, with evidence (identical fills with and without extra tail length on the 5 sequences). This gate is hard; a corrupted Part 0 silently corrupts everything behind it.
Projection gate. On 5 sequences, observed positions return bit-identical after denoising and the filled position is a real vocabulary token, never the MASK id.
Scale. Load both louaaron/sedd-small and louaaron/sedd-medium through the already-patched clone; medium passes the same gates.
Corpus unification. Rerun the corrected linearization diagnostic on EXACTLY the kl_baselines sequence set (same load_texts, build_corruption, seed_all, data_seed 0, n=200), at BOTH scales, sigma 0.1. From here on, linearization, recovery, and hybrid share one corpus and one sequence set. Keep all SEDD runs out of the AR reconcile globs.

PART P1: SEDD NATIVE FLIPPED-TOKEN RECOVERY (both scales). New --exp sedd_recovery under the harness contract (run_name, out_dir, atomic JSON, per-item CSV, shardable). Sequences and corruption exactly the kl_baselines set; verify the first five corrupted sequences match the rev_klbase CSV before scaling. Method: absorbing-SEDD conditional denoising, corrupted positions initialized to MASK, observed positions clamped every step via the repo's projection-function conditioning; repo default sampler settings per scale, recorded in the JSON. Metrics: exact-match % and top-5 % against ground truth, and avg KL under gpt2sft via avg_kl_for_fill (positions are mid-sequence, so it applies), bootstrap CIs over sequences. Print reference rows from rev_klbase_gpt2sft.json next to it: ground_truth 0.00, cond_topk_rescore 4.43, gibbs 6.69, untouched 9.14, flagship Langevin ~6.5. Pre-registered predictions: recovery clearly below untouched at both scales, exact match far above the Langevin arms' 0%, medium at or above small on every metric. Claims are comparative and qualitative; "beats the domain-tuned AR baselines in absolute terms" is NOT the claim and must not be written even if a number allows it, because scale and domain gaps make that comparison unfair in both directions. An outcome above untouched means broken conditioning, not diffusion failure: stop and debug.

PART H: THE HYBRID SAMPLER (both scales). The sufficiency experiment: the thesis's own MH machinery with only the direction signal replaced. Position-wise MH chain on the same sequences and masked positions as P1. Each step: mask the target position, one SEDD forward at sigma 0.1 (the validated linearization readout) gives the log-preference over the vocabulary at that position; renormalize over the REAL vocabulary excluding the MASK id; propose from softmax (temperature 1.0, recorded); accept with the EXACT AR energy from gpt2sft plus the independence correction, log a = (S' - S) + (log q(cur) - log q(prop)). 50 steps, same metrics as P1 plus acceptance rate. Pinned comparison class, pre-registered: the hybrid is a SAMPLER; its opponents are the other samplers under the same exact energy, i.e. the AR left-conditional independence sampler (31.5% exact, 30.3% acceptance at middle positions in rev_last_token_gpt2sft.json), the random arm (0.0%), and the AR-gradient arm (0.0%). cond_topk_rescore is an argmax picker, shown for context only. If the masked-position distribution of the P1 set differs materially from the last_token middle condition, rerun the left-conditional and random reference arms on the P1 sequences so the comparison is same-task, and say in the log whether this was needed. Predictions: acceptance well above the random arm; exact match above zero and at or above the left-conditional's, because the SEDD proposal sees both sides of the mask. If the hybrid does not beat the left conditional, report it straight: that is a real finding about what bidirectionality adds beyond exact left-context reweighting. The sentence the row exists to support: swapping only the direction signal, inside the same MH-corrected chain on the same energy and task, turns a never-recovers sampler into a working one; Phase 2's zero-gradient theorem is the necessity half, this is sufficiency.

PART G: GUIDED GENERATION WITH A TRAINED NOISY CLASSIFIER (medium only).

Train the noisy classifier: take the labeled sentiment data behind the existing sentiment head (reuse the loading path in train_sentiment_head.py), corrupt each example with SEDD's forward absorbing process at a random noise level uniform over the schedule, add the MASK id to the classifier's vocabulary, fine-tune a small classifier (same architecture as the existing head unless masked input forces a change) to predict the label from the noisy state, noise level as an input feature if trivial. One GPU. Log the training curve and held-out accuracy at several noise levels; if held-out accuracy at LOW noise is not well above chance, stop: guidance built on a broken classifier is noise.
Guide at commitment events only: whenever a position commits from MASK to a token during denoising, take SEDD's top-k candidates (k=32, recorded), score the k candidate states with the noisy classifier in one batched pass, multiply the posterior by the classifier ratio for the target label (guidance weight gamma recorded; 1.0, with a small sweep {0.5, 1, 2} only if time allows), renormalize, commit. This keeps classifier passes linear in sequence length.
Task and evaluation: the concern-11 task, both target labels, guided vs unguided SEDD generation. STRICT ROLE SEPARATION, non-negotiable: the new noisy classifier ONLY guides; the steering gain is measured by EXACTLY the evaluator that produced the concern-11 numbers, one fixed judge across the AR arms and this one. Also report KL under gpt2sft for guided and unguided so steering gain and fluency cost appear together. Bootstrap CIs, both labels, paired honesty as in concern 11.
Fairness sentence for the write-up: the AR arms also used a trained classifier; the only difference is that this one is trained on the noisy states it is evaluated on, which is the thesis's own principle applied in the constructive direction. Predictions: guided beats unguided on the evaluator at both labels with bounded KL cost; if the gain is small or asymmetric, report exactly that.

PART T: THESIS INTEGRATION (Doc/), after or interleaved with the shards.

A20: replace the TODO block in the last-token section with the real content: the position-condition table from rev_last_token_gpt2sft.json, the zero-gradient measurement, the 100% independence acceptance and what it certifies, the second-to-last finding, and the closing figure (last_token_figure.png or a thesis-quality regeneration from the CSV). Base the prose on the working-vs-failing paragraph in the Phase 2 log, edited into the chapter voice.
A8 upgrade from designed-but-not-run to done: the linearization table (AR vs SEDD by stratum, unified corpus, both scales), then the capability pilots: P1, H with its own paragraph as the sufficiency half of the argument (it belongs in the why-spine, not buried in a table), and G as trained-guidance steering where the AR constraint gradient steered nothing. Pilot framing throughout, scale and domain caveats stated, small-to-medium presented as robustness.
Concern 6a: reattribute the 0.03% / 3.7% acceptance pair to the no-MH configuration (the matching trace), add the mh=True pair (0.627% / 8.56%) alongside, remove the %TODO-AUTHOR marker, unless the author supplies a different provenance in this session.
Two coherence edits, decided with the author: (a) Introduction Section 2.4 currently asserts the training-free advantage ("enables zero-shot transfer... requiring only the definition of an energy function") as fact; reframe it as the hypothesis under test, and add the closing Discussion sentence that the tested promise failed for a structural reason and the pilot shows what the capability actually costs, namely training for it. (b) State every AR-vs-diffusion revision contrast at the level of the OPERATION, not the pass: within one pass absorbing diffusion also commits; the difference is that revising token i means re-masking and re-denoising, an in-distribution bidirectional query for SEDD, while an AR model has no conditioning path from the right context at all. Never write "diffusion can revise and AR cannot" without this qualifier.
Thread the new results into the why-spine (energy fine, gradient causally blind, score training restores the signal, the repaired signal works inside the same machinery, trained guidance steers) and update the RQ-to-subsection map. Every new number carries its % source comment and appears in the lists of tables and figures.
Recompile clean, render the changed pages and bibliography page 2 to PNG and inspect them, rerun the numbers diff against results_revision/numbers.json plus every new JSON, and append the final status to REVISION_LOG.md with anything still needing the author.

Constraints: no experiment of the sample-many-and-filter shape, under any name. Nothing beyond this slate without stating the plan and waiting. Do not touch core/ samplers. Doc/ edits keep removed text in % comment blocks until the author confirms. Established voice, no formulaic scaffolding, no em-dashes. If any gate fails or any outcome contradicts its pre-registered prediction, stop, report, and do not write it into the thesis until the cause is understood.