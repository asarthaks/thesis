\section{Appendix}
\label{ch:appendix}

\subsection{Additional Diagnostic Figures}
\label{app:figures}

This section collects supporting figures referenced in Section~\ref{ch:results}. Each figure is discussed at the point of reference in the results section, as indicated in the captions below.

% SOURCE: figures/fig_lin_decomposition.pdf produced by plot_diagnostics.py
% from results/diagnostics/diag/diag_linearization_gpt2sft.csv (self/future decomposition).
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/fig_lin_decomposition.pdf}
    % SIZING (A4 print legibility): source 691 x 328pt, aspect 0.475, so full text width is 203pt tall.
    \caption[Self and future terms of the true energy change.]{The true energy change decomposed into its self term and its future term, each plotted against the gradient surrogate, for GPT-2 Large.}
    \label{fig:lin-decomp}
\end{figure}

% SOURCE: figures/fig_lin_topk.pdf produced by plot_diagnostics.py
% from results/diagnostics/diag/diag_linearization_gpt2sft.csv (top-k recall vs random).
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.70\textwidth]{figures/fig_lin_topk.pdf}
    % SIZING 2026-07-27: 0.92 -> 0.70 textwidth so that Figures 7 and 8 share one page.
    % Source 384 x 272pt, so at 299pt wide it is 212pt tall; with Figure 8 (204pt) and both
    % captions the pair fits inside the 591.5pt text height. Scale on the page is 0.78 of the
    % source, so the axis labels remain legible in A4 print.
    \caption[Top-$k$ recall of gradient ranking against a random baseline.]{Recall of the true top-$k$ token substitutions by the gradient-ranked top-$k$, against a random-ranking baseline, for GPT-2 Large.}
    \label{fig:lin-topk}
\end{figure}

% SOURCE: figures/fig_mh_accept.pdf produced by analyze_mh.py --config cls_policy_gnoff_mh
% from results/diagnostics/diag/traces_gpt2sft_mh.csv (acceptance conditioned on boundary
% crossing, for the single continuous policy configuration only). Part 1, Alarm 1: the
% earlier figure pooled three configs (cls gnoff/gnon MH plus the dls control) and reported
% a misleading combined bar (stayed 0.064, crossed 0.074); regenerated for one config 2026-07-26.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.70\textwidth]{figures/fig_mh_accept.pdf}
    % SIZING 2026-07-27: paired with Figure 7 on one page, see the note there.
    \caption[Acceptance rate conditioned on boundary crossing.]{Metropolis--Hastings acceptance rate for the continuous policy sampler with gradient normalization off and the correction enabled, for that single configuration rather than pooled, conditioned on whether the proposal crossed a Voronoi cell boundary.}
    \label{fig:mh-accept}
\end{figure}

% SOURCE: figures/fig_trap_scatter.pdf produced by plot_diagnostics.py
% from results/diagnostics/diag/diag_likelihood_trap_gpt2sft.csv.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/fig_trap_scatter.pdf}
    % SIZING (A4 print legibility): source 587 x 322pt, aspect 0.548, so full text width is 234pt tall.
    \caption[Per-token likelihood against repetition and diversity, GPT-2 Large.]{Mean per-token log-probability against 4-gram repetition rate and distinct-2 diversity for GPT-2 Large, one point per generation, coloured by decoding strategy.}
    \label{fig:trap}
\end{figure}

% SOURCE: figures/fig_aniso_hist.pdf produced by plot_diagnostics.py
% from results/diagnostics/diag/diag_anisotropy_*_dists.npz.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/fig_aniso_hist.pdf}
    % SIZING (A4 print legibility): source 603 x 305pt, aspect 0.505, so full text width is 216pt tall.
    \caption[Pairwise token-embedding distance distributions.]{Distributions of pairwise token-embedding distances in GPT-2 Large and Llama-3, plotted on separate scales.}
    \label{fig:aniso}
\end{figure}

% SOURCE: figures/fig_trap_length.pdf produced by plot_diagnostics.py
% from results/diagnostics/diag/diag_likelihood_trap_gpt2sft.csv (length vs total logp regression).
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/fig_trap_length.pdf}
    \caption[Total log-likelihood against generated length, GPT-2 Large.]{Total log-likelihood against generated length for GPT-2 Large. The regression is fitted over the three stochastic decoders, where length varies; the deterministic decoders pinned at the length cap are shown faintly but excluded from the fit.}
    \label{fig:trap-length}
\end{figure}

% CUT (page ledger, ranked cut list: appendix detail first): forced \clearpage removed so
% the subsection may share a page with what precedes it. No content removed.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/gpt2-large.dls.gn.free.s100_new_trajectories.png}
    % SOURCE: figures/gpt2/gpt2-large.dls.gn.free.s100_new_trajectories.png
    \caption[The same sampler over a 100-step schedule.]{Discrete Langevin Sampler trajectories on GPT-2 Large over a 100-step annealing schedule, averaged across sequences. Panels, axes, line styles and markers as in Figure~\ref{fig:dls-traj-50}.}
    \label{fig:dls-traj-100}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/gpt2-large.dls.nogn.free.s50_companion.png}
    % SOURCE: figures/gpt2-large.dls.nogn.free.s50_companion.png from
    % revision/plot_dls_trajectories.py draw(50, ..., gn=False), reading the
    % gpt2-large.dls.{policy,grad_norm_preserved_random_dir,random}.{mh,nomh}.nogn.free.s50
    % grid CSVs (author issue list item 21).
    \caption[The three proposal arms with gradient normalization disabled.]{The 50-step schedule with gradient normalization disabled, in which the three proposal arms separate into distinct curves. Panels and axes as in Figure~\ref{fig:dls-traj-50}.}
    \label{fig:dls-traj-nogn}
\end{figure}

\subsection{Full Configuration Grid}
\label{app:grid}

% SOURCE: figures/compare/final_kl_by_model.csv (all 29 configuration rows x 5 models).
Table~\ref{tab:full-grid} reports the final KL divergence for a representative subset of the full configuration grid across the four GPT-2-family energy functions. The complete grid comprises the factorial combination of sampler (DLS, CLS), proposal method (policy, grad-norm-preserved, random), Metropolis--Hastings correction (on, off), gradient normalization (on, off), and schedule length (50, 100), evaluated on each of the five energy functions, for a total of $145$ configurations.

The count arises as $145 = 5 \times 29$, and the twenty-nine is worth setting out because the naive factorial overcounts it. A fully crossed design over the five axes above would give $2 \times 3 \times 2 \times 2 \times 2 = 48$ cells per energy function, but two prunings reduce this: the continuous sampler is run only for the policy and random methods and across four of the gradient-normalization and schedule variants, giving $8$ CLS configurations, while the discrete sampler is run for all three methods across seven of the correction, normalization and schedule variants, giving $21$. The sum is $29$ per energy function, and counting the completed runs confirms it: $29$ configurations for GPT-2 Large, $29$ for Llama-3 and $87$ across the three GFlowNet variants.
% SOURCE: REVISION_LOG.md concern 15; run-name counts in results/grid/gpt2_v2 (29),
% results/grid/llama (29), results/grid/gfn (87). The nominal 2x3x2x2x2x5=240 overcounts because
% CLS drops the grad-norm-preserved method and some gn/schedule cells.

\begin{table}[htbp]
    \centering
    \scriptsize
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lcccc}
        \toprule
        Configuration & GPT-2 Large & GFN lb0-500 & GFN lb0-2000 & GFN lb1-500 \\
        \midrule
        DLS policy, corr., grad-norm, 50 steps & $6.541$ & $6.306$ & $6.721$ & $6.415$ \\
        DLS policy, no corr., grad-norm, 50 & $9.499$ & $9.195$ & $10.085$ & $8.669$ \\
        DLS random, corr., grad-norm, 50 & $6.370$ & $6.105$ & $6.548$ & $5.525$ \\
        DLS norm-preserved random, corr., grad-norm, 50 & $6.370$ & $6.105$ & $6.548$ & $5.525$ \\
        DLS policy, corr., no grad-norm, 50 & $6.474$ & $6.009$ & $6.698$ & $6.008$ \\
        DLS norm-preserved random, corr., no grad-norm, 50 & $6.430$ & $6.001$ & $6.552$ & $5.338$ \\
        CLS policy, corr., no grad-norm, 50 & $9.202$ & $8.834$ & $9.748$ & $9.025$ \\
        % SOURCE (Part 1, Alarm 2): final_kl_by_model.csv row cls.policy.mh.nogn.free.s50
        % = 9.202 / 8.834 / 9.748 / 9.025 (gpt2-large, gfn-lb0-500, gfn-lb0-2000, gfn-lb1-500);
        % grid JSON gpt2-large.cls.policy.mh.nogn.free.s50.json mean_kl[-1] = 9.20179.
        % The submitted table duplicated the no-MH row's numbers here; corrected 2026-07-26.
        CLS policy, no corr., no grad-norm, 50 & $8.083$ & $7.799$ & $8.198$ & $13.732$ \\
        \bottomrule
    \end{tabular}
    \caption[Representative subset of the final-KL configuration grid.]{Representative subset of the final-KL configuration grid. Rows are configurations, columns are the four GPT-2-family energy functions; ``corr.'' is the Metropolis--Hastings correction and ``grad-norm'' gradient normalization.}
    \label{tab:full-grid}
\end{table}

The eight rows above are a subset chosen to show the axes that matter, the two samplers against the two settings of the correction and of gradient normalization. The remaining configurations behave as these do, and the arithmetic above rather than the table is what establishes the count of $145$.

% CUT (page ledger, ranked cut list: appendix detail first): forced \clearpage removed so
% the subsection may share a page with what precedes it. No content removed.
\subsection{Computational Cost}
\label{app:cost}

Because gradient-free methods match or beat the gradient-guided samplers on the same energy, their computational cost is set out here; it differs by more than a constant factor. Table~\ref{tab:cost} gives the number of network evaluations each method spends to recover a masked span, counted as forward and backward passes, where $S$ denotes the schedule length (either $50$ or $100$), $M$ the number of masked positions, $T$ the number of Gibbs sweeps, and $k$ the number of candidates a rescoring pass considers.
% SOURCE: pass counts from core/base_sampler.get_gradient_and_log_joint (one forward +
% one backward per gradient evaluation), core/dls.py and core/cls.py (_step calls it once
% per step, and again at the proposed point when mh_sampling is on), and the baseline
% definitions in diagnostics/run_revision.py.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lccp{3.9cm}}
        \toprule
        Method & Forward & Backward & Note \\
        \midrule
        DLS / CLS, no correction  & $S$    & $S$  & one gradient per step \\
        DLS / CLS, with correction & $2S$   & $2S$ & extra gradient at the proposed point \\
        Conditional argmax/sample  & $1$    & $0$  & a single forward pass \\
        Top-$k$ rescore            & $1+k$  & $0$  & rescore $k$ candidates \\
        Gibbs                      & $M\,T$ & $0$  & $M$ positions, $T$ sweeps, no gradient \\
        \bottomrule
    \end{tabular}
    \caption[Network evaluations per recovered span.]{Network evaluations per recovered span, counted as forward and backward network passes.}
    \label{tab:cost}
\end{table}

The measured wall-clock times are consistent with this accounting and give a sense of the absolute scale on the hardware of Section~\ref{sec:meth-repro}. The discrete sampler on the free-form continuation task took $2157$ seconds for one hundred sequences, about $21.6$ seconds per sequence over a fifty-step schedule; the full suite of gradient-free baselines, including the Gibbs sampler, took $215$ seconds for two hundred sequences, roughly one second per sequence; and rescoring the six hundred recovered sequences with the external Llama-3 judge, a single forward pass each, took $24.8$ seconds in total.
% SOURCE: rev_continuation_gpt2sft.json wall_time_sec 2157.4 (n=100, span=20, steps=50);
% rev_klbase_gpt2sft.json wall_time_sec 215.3 (n=200, all baselines incl. gibbs);
% rev_judge_score_gpt2sft.json wall_time_sec 24.8 (n=600 judge forward passes).
The comparison is stark: the gradient-free baselines reach comparable or better recovery quality at a fraction of the per-sequence cost, because they never pay for the backward pass whose product this thesis finds to be uninformative.

% CUT (page ledger, ranked cut list: appendix detail first): forced \clearpage removed so
% the subsection may share a page with what precedes it. No content removed.
\subsection{Geometry of Sampler Trajectories in Embedding Space}
\label{app:trajectory}

This section examines the spatial paths the samplers trace through the embedding space, a concrete counterpart to the acceptance statistics and trajectory summaries of Section~\ref{sec:results-mh}. The primary figure is deliberately \emph{not} a projection, since a two-dimensional projection of a $1280$-dimensional space cannot carry the argument; the load-bearing figure reports exact $L2$ distances in the full space, using the full $50{,}257\times 1280$ embedding matrix rather than any subsample, and the projection is demoted to one illustrative panel.

Figure~\ref{fig:traj-distance} is the primary view. For one trace, index $5$, drawn once under a fixed random seed of zero and not reselected, it plots at every step, on a symmetric-log axis so exact zeros remain visible, the $L2$ distance from the sampler's state to the ground-truth token embedding, and for the continuous sampler also the distance to the nearest token of any kind.
% SOURCE: figures/fig_traj_stats.json (seq_idx_seeded 5) and revision/plot_trajectories.py, from results/diagnostics/diag/traces_gpt2sft_plot_traj.npz.
Because the trace-collection procedure advances the random-number state from one configuration to the next, trace index $5$ is the same source sentence in every panel, but its randomly masked position, and hence its ground-truth token, differs by configuration; each panel therefore uses its own exact ground-truth embedding, and the cross-panel claim is about the on-manifold-versus-off-manifold dynamics, which does not require a shared token. The token strip under each axis shows the decoded token at each change, and for the continuous sampler the steps at which the proposal was rejected, inferred exactly from identical consecutive states, are marked as ticks.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/fig_traj_distance.png}
    % SOURCE: figures/fig_traj_distance.png from revision/plot_trajectories.py
    % (traces_gpt2sft_plot_traj.npz + full wte from the gpt2sft checkpoint).
    % G2 (2026-07-28), PARTIAL FIX, and the limitation is recorded rather than hidden. This
    % figure is authored at 8.4in and was printed at 0.80\textwidth (4.7in), a 0.56x
    % reduction that put its 9pt axis labels near 5pt and its 7pt legend near 4pt. The proper
    % fix is to redraw it at the printed width, but revision/plot_trajectories.py needs the
    % full 50257x1280 embedding matrix from the GPT-2 Large SFT checkpoint, and that
    % checkpoint is no longer on disk (gfn-lm-tuning/gpt2_large_sft_output is empty), so the
    % figure cannot be regenerated in this pass. Widening it to the full text block is the
    % available mitigation: it raises the effective sizes by a quarter, to roughly 6.3pt and
    % 4.9pt, which is better but still under the 8pt floor. Flagged for the author.
    \caption[Full-space distances along one seeded sampling trajectory.]{Full-space distances along the sampling trajectory for one trace, index $5$, one panel per configuration.}
    \label{fig:traj-distance}
\end{figure}

The distances, which Section~\ref{sec:results-traj} summarizes, make the geometry exact. The discrete sampler ends about $2.2$ units from the ground truth, a token or so away, and it shows the same dependence on the correction as the continuous one but without the off-manifold cost: with the correction it visits about $5$ of $50$ cells, and with the correction removed about $49$, accepting almost every proposal, yet it never leaves the token manifold, so its wandering is among real tokens rather than through the empty space between them.
% SOURCE: figures/fig_traj_stats.json per_config dls_policy_gn_mh / dls_random_gn_mh (nearest_token_dist 0.0; distinct_cells_mean 5.3/4.2; final_dist_to_gt_mean 2.16/2.36).
% SOURCE (Phase 9, author issue list item 23): fig_traj_stats.json per_config
% dls_policy_gn_mh distinct_cells_mean 5.33 (MH on) and dls_policy_gn_nomh distinct_cells_mean
% 48.67 (MH off, from traces_gpt2sft_dlsnomh; same 6 source sentences, independently seeded
% corruption; DLS on-manifold, nearest-token distance 0).
% SOURCE: figures/fig_traj_stats.json per_config cls_policy_gnoff_mh (nearest 117.6-151.3, distinct_cells 2.2) and cls_policy_gnoff_nomh (nearest max 978.7, distinct_cells 44.7, end_nearest 17.0).
The continuous state therefore spends its run in essentially empty space where the projected energy is a poor guide, which is why the boundary-crossing correction almost always rejects.

Figure~\ref{fig:traj-pca} is the secondary, illustrative view: the same trace projected onto the first two principal components of the full vocabulary embedding matrix. It is shown for intuition only, and it cannot be the evidence: the two components capture just $3.3\%$ of the embedding variance, so the projection discards almost all of the geometry the distances above measure exactly.
% SOURCE: figures/fig_traj_stats.json pca_explained_variance_sum 0.0331.
Read for what it can show, it agrees with the distances: the discrete trajectories stay inside the vocabulary cone while the continuous ones escape far outside it.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/fig_traj_pca.png}
    % SOURCE: figures/fig_traj_pca.png from revision/plot_trajectories.py.
    % G2 (2026-07-28), PARTIAL FIX, same cause and same limitation as Figure 14 above:
    % authored at 9.2in, printed at 0.85\textwidth. Widened to the full text block; it cannot
    % be redrawn without the missing checkpoint.
    \caption[PCA projection of the same seeded trace.]{PCA projection of the same trace, one panel per configuration. Grey cloud: the vocabulary embeddings. Open circle: the start state. Filled dot: the end state. Star: the ground-truth token.}
    \label{fig:traj-pca}
\end{figure}

Read together, the two figures make the central geometric claim visible: the samplers either fail to move in any directed way, the discrete sampler and the free continuous sampler both wandering without bending toward the ground-truth token, or fail to move at all, the corrected continuous sampler frozen far off the manifold, and in neither case does the path converge on the target, which is the spatial signature of a gradient that carries no directional signal.

% CUT (page ledger, ranked cut list: appendix detail first): forced \clearpage removed so
% the subsection may share a page with what precedes it. No content removed.
\subsection{Classifier-Guided Steering: Full Results}
\label{app:guided}


This subsection reports in full the exploratory follow-up summarized in
Section~\ref{sec:results-guided}. It asks whether the constructive direction of
Section~\ref{sec:results-ladder} extends to \emph{control}, which broadens the study from a
sampling diagnosis into classifier-guided diffusion and evaluator alignment, and it is
reported as exploratory throughout.

A small classifier is trained to read a noisy, partially masked state, the state the diffusion model occupies mid-denoising, by corrupting each labelled example with the model's own forward absorbing process at a noise level drawn uniformly over the schedule. Its held-out accuracy at low noise is $81\%$.
% SOURCE: noisy_classifier_train.json final_low_noise_acc=80.96.
During generation it reweights the diffusion model's top thirty-two candidates at each commitment by the probability of the target label, raised to a guidance strength $\gamma$. Steering is scored, exactly as in Section~\ref{sec:results-constrained}, by the frozen-model sentiment judge, so the classifier that guides and the judge that grades are strictly separate. Two settings were run: an off-domain continuation setting on prompts neither classifier was calibrated for, and an on-domain setting on held-out SST-2 prompts with a fluency trust region that caps guidance to candidates within $\delta=5$ nats of the diffusion model's own top choice. Table~\ref{tab:gprime} reports the second, and the sources for the four summary findings are as follows.
% SOURCE: rev_gprime.json by_cell.*.self_gain_pts and self_gain_ci95; rev_sedd_guided_g{1,2,4}.json by_label.*.self_gain_pts.
% SOURCE: results/revision/rev_gprime.json diagnosis_unguided_gen_agreement_pct 71.667 [66.667,76.667]; realtext_agreement_pct 79.667 [75,84]; phase4_offdomain_agreement_band_pct [56,64]; by_cell.*.gain_pts and gain_ci95.
% SOURCE: rev_gprime.json by_cell.*.nll_cost (-0.74, -1.71, +0.26, -1.00); unguided_span_nll 7.014; rev_sedd_guided_g{1,2,4}.json unguided_span_nll -> guided_span_nll.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{llccccc}
        \toprule
        $\gamma$ & target & unguided & guided & judge gain [CI] & self gain & NLL (un$\to$guided) \\
        \midrule
        $2$ & negative & $53.0$ & $50.7$ & $-2.3\ [-7.0, 2.3]$   & $+18.0$ & $7.01\to6.27$ \\
        $2$ & positive & $47.0$ & $53.7$ & $+6.7\ [1.7, 11.7]$   & $+9.3$  & $7.01\to5.30$ \\
        $4$ & negative & $53.0$ & $52.3$ & $-0.7\ [-5.3, 4.0]$   & $+25.7$ & $7.01\to7.28$ \\
        $4$ & positive & $47.0$ & $54.3$ & $+7.3\ [2.7, 12.0]$   & $+17.7$ & $7.01\to6.01$ \\
        \bottomrule
    \end{tabular}
    % SOURCE: results/revision/rev_gprime.json by_cell.{gamma2_label0,gamma2_label1,
    % gamma4_label0,gamma4_label1}: unguided_hit_pct, guided_hit_pct, gain_pts+gain_ci95,
    % self_gain_pts, unguided_span_nll -> guided_span_nll. n=300 pairs per cell.
    \caption[On-domain trust-region guided diffusion generation, held-out SST-2 prompts.]{On-domain, trust-region guided diffusion generation on held-out SST-2 prompts. Judge gain is the guided minus unguided target-hit percentage; self gain is the guiding classifier's own verdict.}
    \label{tab:gprime}
\end{table}

The claim the data licenses is therefore narrow and setting-contingent: with a single noisy guide at this scale, one-directional transfer to an independent judge is reachable under bounded fluency, while symmetric transfer at both labels is not. The off-domain run, the full agreement ladder, and the per-class confusion analysis follow; Appendix~\ref{app:gprime-examples} gives representative guided pairs from both settings.

\subsubsection{Off-Domain Guidance Without a Trust Region}
\label{app:guided-offdomain}

The first setting uses prompts from a MuCoLa-style continuation set that neither classifier
was trained on, and imposes no fluency constraint. Table~\ref{tab:diffusion-guided} reports
it. The mechanism works on the instrument it optimizes, moving the guiding classifier's own
verdict toward the target at every strength and for both labels by seven to eighteen points.
Measured by the independent judge it steers monotonically in the negative direction, from a
six-point gain at $\gamma=1$ to a $16.7$-point gain at $\gamma=4$ whose interval excludes zero,
and it does not move the judge in the positive direction. The two agree on only fifty-six to
sixty-four percent of these texts, and stronger guidance drives them further off the fluent
manifold, the per-token negative log-likelihood rising from $7.1$ to $11.0$ as $\gamma$ grows.
% SOURCE: rev_sedd_guided_g{1,2,4}.json by_label.*.
Guiding hard toward one classifier's notion of a label therefore need not raise the other's,
which is the same off-manifold breakdown that Section~\ref{sec:results-constrained} observed
for the constraint-only arm.

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{lccccc}
        \toprule
        & \multicolumn{2}{c}{Judge target-hit (\%)} & Judge gain & Self gain & NLL/token \\
        \cmidrule(lr){2-3}
        $\gamma$, target & unguided & guided & (pts, [CI]) & (pts) & (un$\to$guided) \\
        \midrule
        $1$, negative & 49 & 55 & $+6.0\ [-1.3,13.3]$ & $+9.7$ & $7.1\to8.5$ \\
        $1$, positive & 44 & 39 & $-5.3\ [-12.3,1.7]$ & $+11.0$ & $7.2\to8.5$ \\
        $2$, negative & 49 & 60 & $+10.7\ [3.3,18.0]$ & $+7.7$ & $7.1\to9.7$ \\
        $2$, positive & 44 & 44 & $+0.0\ [-7.0,7.0]$ & $+12.0$ & $7.2\to9.6$ \\
        $4$, negative & 49 & 66 & $+16.7\ [9.7,24.0]$ & $+7.3$ & $7.1\to11.0$ \\
        $4$, positive & 44 & 39 & $-5.3\ [-13.0,2.7]$ & $+18.3$ & $7.2\to11.0$ \\
        \bottomrule
    \end{tabular}
    % SOURCE: rev_sedd_guided_g{1,2,4}.json by_label.{0,1}: unguided_hit_pct, guided_hit_pct,
    % gain_pts + gain_ci95, self_gain_pts, unguided_span_nll -> guided_span_nll.
    \caption[Off-domain classifier-guided diffusion generation.]{Classifier-guided diffusion generation on the off-domain continuation task. Columns as in Table~\ref{tab:gprime}.}
    \label{tab:diffusion-guided}
\end{table}

\subsubsection{The Guide-Judge Agreement Ladder and Per-Class Confusion}
\label{app:guided-ladder}
\label{app:guided-confusion}

The off-domain result leaves two explanations entangled. Guidance might steer only in one
fixed direction, or the two independently trained classifiers might disagree on off-domain
text they were never calibrated for, with stronger guidance driving the text further off the
fluent manifold where disagreement is worst. The continuation run varies domain and fluency
together and cannot separate them. The on-domain control holds fluency fixed and moves the
instruments on-domain: its prompts are the first three hundred held-out SST-2 validation
sentences of at least twelve tokens, on which neither classifier trained, both having seen the
split only for an accuracy readout with no parameter update, so it is calibration-clean by
construction. The trust region caps guidance to candidates within $\delta=5$ nats of the
diffusion model's top choice at each commitment, so guidance can only reshuffle mass among
near-equally-fluent options, and unguided and guided generations share a seed per prompt, so
each pair differs only by the intervention.

The agreement between the guiding classifier and the judge climbs a clean ladder as the text
they score moves on-domain: from the off-domain band of $56$ to $64\%$, to $71.7\%$
(95\% CI $[66.7, 76.7]$) on the unguided on-domain generations, to $79.7\%$
($[75.0, 84.0]$) on the real held-out sentences themselves, where the judge and the guide
reach $88\%$ and $79.7\%$ accuracy against the gold labels.
% SOURCE: results/revision/rev_gprime.json diagnosis_unguided_gen_agreement_pct 71.667 [66.667,76.667]; realtext_agreement_pct 79.667 [75,84]; realtext_judge_acc_pct 88.0; realtext_clf_acc_pct 79.667; phase4_offdomain_agreement_band_pct [56,64].
The two classifiers are therefore not miscalibrated in a fixed way; they agree more the closer
the scored text sits to the distribution they were trained on, which is what the off-domain
diagnosis predicted.

The working direction flips between the two settings: off-domain the judge moved on the
negative target and not the positive, on-domain the reverse, with fluency no longer a
confound. That flip is what identifies the residual limit. It is not a fluency artifact,
because the trust region removed the fluency cost and the asymmetry remained, and it is not a
fixed unreachable sentiment, because the reachable direction moved when the setting changed.
What remains is an instrument-alignment effect: the two independently trained classifiers
still disagree on about $28\%$ of the on-domain generations, and steering toward one
classifier's notion of a label registers on the other only where the two align on the
generated text.

% CUT (page ledger, ranked cut list: appendix detail first). Subsection A.5.3, "Per-Class
% Confusion on the Neutral Calibration Surface", was merged into A.5.2 here: its heading is
% removed and its two paragraphs are consolidated into the one below, which had already
% stated the alignment gap they restated. Every number, the table and the source comments are
% retained. T1 also applies: the merged heading takes the document's title-case convention.
One explanation of the flip is that the residual disagreement falls asymmetrically by class,
so that the guide's positive verdict transfers more reliably than its negative one.
Table~\ref{tab:confusion} tests that on the unguided on-domain generations, which are neutral
with respect to the intervention, and does not support it. The per-class accuracies rule out a
silent label-map flip: the guide is $79.4\%$ accurate on negative and $79.9\%$ on positive
held-out sentences, the judge $85.8\%$ and $89.9\%$, both balanced.
% SOURCE: results/revision/rev_confusion.json label_alignment_sanity.
On the unguided generations the judge confirms the guide's positive verdict $67.9\%$ of the
time and its negative verdict $75.7\%$, a paired difference of $-7.7$ points whose $95\%$
confidence interval $[-18.0, +2.6]$ includes zero, so the two conditional agreement rates are
statistically indistinguishable. No by-class asymmetry explains the transfer direction, which
is therefore reported as observed and setting-contingent, with no class-level mechanism
claimed beyond the alignment gap.
% SOURCE: results/revision/rev_confusion.json confusion_unguided_ondomain: P_judge_pos_given_guide_pos 67.95 [60.4,75.3], P_judge_neg_given_guide_neg 75.69 [68.5,82.4], conditional_diff -7.75 [-18.0,2.6].

\input{chapters/tab_confusion}

% CUT (page ledger, ranked cut list: appendix detail first): forced \clearpage removed so
% the subsection may share a page with what precedes it. No content removed.
\subsection{Raw Steering Gains for the Constrained Extension}
\label{app:constrained-raw}

Table~\ref{tab:constrained} gives the raw target-hit gains for the constrained-generation extension. They are placed here rather than in Section~\ref{sec:results-constrained} because that section discounts them: every arm, including the pure-fluency and random-direction baselines, flips sign with the target label, which shows that a fixed unconditional sentiment drift dominates the raw gain. The trustworthy statistic is the paired contrast of Table~\ref{tab:constrained-contrast}, which cancels that shared drift.

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{lcc}
        \toprule
        Constraint mode & Steering gain & Final KL \\
        \midrule
        Fluency only & $0.0$ & $8.558$ \\
        Constraint only & $-8.0$ & $8.308$ \\
        Combined & $-1.0$ & $8.541$ \\
        Fully random & $-7.0$ & $8.512$ \\
        Randomized constraint & $+1.0$ & $8.528$ \\
        \bottomrule
    \end{tabular}
    \caption[Steering effect of the sentiment constraint, discrete sampler, infilling.]{Steering effect of the sentiment constraint under the discrete sampler on the infilling task.}
    \label{tab:constrained}
\end{table}

\subsection{Qualitative Examples}
\label{app:gprime-examples}
\label{app:showcase}

All qualitative material in this thesis is collected here under one selection policy, so that no example shown anywhere was chosen for how well it made a point. Every draw below is taken under a fixed random seed of zero and is unfiltered: dull, crude and failing draws are kept, and the only subsequent pruning is deterministic, a fixed positional subset of the draw, stated at each table. No number in this thesis is taken from these tables; the statistics are in Table~\ref{tab:baselines}, Table~\ref{tab:mlm}, Table~\ref{tab:gprime} and Table~\ref{tab:confusion}. Garbled bytes that the diffusion model emits are shown as \texttt{[?]}.

\subsubsection{Classifier-Guided Generations}
\label{app:examples-guided}
The pairs below make the guided steering of Section~\ref{sec:results-guided} visible in text. Table~\ref{tab:gprime-examples} gives the on-domain, trust-region generations, one pair per cell. For each pair \emph{g} is the guiding classifier's verdict and \emph{j} the independent judge's, so a pair where they disagree shows the partial instrument alignment directly.
\input{chapters/gprime_examples}

% CUT (page ledger, ranked cut list: appendix detail first): forced \clearpage removed so
% the subsection may share a page with what precedes it. No content removed.
\input{chapters/showcase_appendix}

% CUT (page ledger, ranked cut list: appendix detail first): the last forced \clearpage
% removed so A.8 may share a page with the showcase tables. No content removed.
\subsection{Use of AI Tools}
\label{app:ai-tools}

The author of this thesis has used AI tools, specifically Google AI Studio (Gemini) and Claude (Anthropic), and documents their use here as required.

They were used in three ways. First, and most substantially, for engineering support: the tools helped build the infrastructure that runs the experiments across several GPUs in parallel, and that generates the manifest files and the queueing mechanism which make the long configuration grids reported here practical to run. Second, for debugging, once the experimental code had been written by the author: the tools were used to check that the implemented samplers agreed with the mathematics they are derived from, and to locate the places where they did not. Third, on the manuscript itself, after the text had been written by the author: the tools were used through an agentic coding interface to edit the LaTeX sources and lay the document out quickly, and in the same pass to paraphrase and copy-edit drafts.

All other parts of the thesis, including the design of the experiments, the synthesis of the background and related work, the analysis and interpretation of the results, and the arguments and conclusions drawn from them, were carried out solely by the author, who has verified all quantitative claims against the underlying experimental output and assumes full responsibility for the entire content of this work.
