\thispagestyle{empty}
\begin{center}
    {\Large\bfseries Abstract}
\end{center}

% A4 (supervisor, highest-priority remark): "Abstract requires some work to be understandable.
% Better leave stuff out. Intro can contain it. ... less, but clear is more." Rewritten to be
% readable without the introduction: every term is glossed where it is first used, the
% compressed chains of reasoning are broken into plain sentences, and material was CUT rather
% than compressed further. Everything cut is stated in Section 1 (introduction) and again in
% the chapter that reports it. What was cut FROM THE ABSTRACT, and where it still stands:
%   - the three gradient-free baseline figures (top-k rescore 4.43, Gibbs 6.69, best Langevin
%     cell 6.4): Section 5.3 and Chapter 7. The claim they supported is kept here in words.
%   - the "5 x 5" shape of the step-size and temperature sweep: Section 5.3.2. The span it
%     covers (uniform to gradient-determined) is kept here in words.
%   - the two explanatory clauses on how the GFlowNet policies failed (reward exploitation;
%     tuned energy indistinguishable from the base): Section 1.3, Section 5.9, Chapter 7.
%   - the three-hypothesis elimination structure, which merged into finding (2) here and is
%     set out in full in Section 1.2.
% No number's value is altered anywhere by this rewrite.
% A1: one footnote giving a concrete constraint example (a length constraint).
% A2: the five energy functions are now named individually.
% A3: the findings are numbered in the text so they can be referred to. They are inline
% WITHDRAWN on the author's instruction, 2026-07-28. Numbered findings do not occur in
% abstracts in this field and the reference thesis in refs/IoannaThesis/ has none, so the
% markers are removed and the results run on as continuous prose. The supervisor's "Number
% them in text" sits directly beside "5 energy functions given by 5 different LLMs?", so the
% enumeration he asked for is most plausibly of the energy functions, which paragraph two
% now supplies by naming all five. Register throughout follows that reference thesis: long
% declarative sentences, "i.e." glosses, "Results show that", "These findings indicate that".
{\renewcommand{\baselinestretch}{1.12}\normalsize
\noindent Controllable text generation asks a language model for text satisfying a property of the sequence as a whole, which cannot be verified until the sequence is complete.\footnote{A length constraint is the simplest example: whether a sentence stays under twenty words is settled only once the sentence is finished.} One family of controllable generation methods pursues this without retraining the model, by treating its frozen likelihood as an \emph{energy function}, i.e. a score that is low for text the model considers good, and sampling from that score with Langevin dynamics, which repeatedly nudges the text in the direction the gradient recommends, with an additive constraint supplied at generation time. This thesis tests the assumption on which that construction rests, namely that the gradient indicates which token should be placed where.

\noindent The evaluation uses masked token recovery, in which one token of a real sentence is replaced at random and the sampler must restore it, over $145$ configurations and five frozen energy functions: a GPT-2 Large fine-tuned on stories, three variants of it tuned further with a GFlowNet, which learns to generate in proportion to a reward, and a Llama-3 8B as a cross-architecture control.

\noindent Results show that the direction of the gradient is not usable: replacing it with a random direction of the same magnitude produced no reliable difference, an equivalence certified over $1000$ paired sequences, the correct token was recovered in $0.0\%$ of attempts in $139$ of the $145$ configurations, and a sweep over the settings that make the gradient decisive found none at which it helped. The failure is attributable to the coordinate in which the derivative is taken, since differentiating the same likelihood with respect to the token's indicator over the vocabulary rather than its input embedding introduces the candidate's own conditional log-probability, to which that gradient is structurally blind; in the same sampler and on the same weights that derivative recovers $40\%$ of tokens on GPT-2 and $41\%$ on Llama-3, where the input-embedding gradient never exceeds $2\%$. The gap that remains is associated with conditioning rather than training, as a single chain accepting by the exact energy and varying only the proposal recovers $0.5\%$ from a uniform draw, $23.5\%$ from the model's own left-to-right conditional, $39\%$ from a diffusion model trained to estimate scores, and $44.5\%$ from a masked language model never trained on scores. These findings indicate that inference-time control on a frozen autoregressive model should differentiate the token indicator, or draw proposals from the output distribution, rather than from the input embeddings.

\par}

\clearpage
